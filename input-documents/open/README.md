# Open Questions

This directory holds input documents that surface unknowns or decisions that need to be resolved before a specific documentation change can be proposed.

Once a change proposal that addresses an open question is successfully implemented via `/apply-inputs`, the open question document is deleted.

See `../README.md` for the open question document format.
